# PLACEHOLDER — this is NOT yet a real sops file.
#
# Replace it once the age recipients in ../.sops.yaml are filled in:
#   sops secrets/k3s.yaml
# and set the value (from the old ~/.token.k3s on the running cluster):
#   k3s-token: <token>
#   flux-deploy-key: <OpenSSH private key used as the Git deploy key>
#   flux-sops-age-key: <AGE-SECRET-KEY-... used by Flux to decrypt manifests>
#
# After it is a real encrypted file, flip `sops.validateSopsFiles` back to
# `true` in modules/nixos/sops.nix.
k3s-token: REPLACE_ME
flux-deploy-key: |
  REPLACE_ME
flux-sops-age-key: |
  REPLACE_ME
